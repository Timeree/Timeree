proxies:
  - name: "GithubCodespaceProxy"
    type: socks5
    server: 127.0.0.1
    port: 1080

proxy-groups:
  - name: "Global"
    type: select
    proxies:
      - GithubCodespaceProxy

rules:
  - GEOSITE,CN,DIRECT
  - MATCH,Global
  